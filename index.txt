git init
git add index.html
git commit -m "first commit"
git branch -M master
git remote add origin https://github.com/GdfyOphelie/group_project1.git
git push -u origin master


<p>git init pour initialiser le .git il est masquer</p>
    <p>Pour enregistrer en 2 etape</p>
    <p>git add index.html</p>
    <p>git commit -m "prolject.init"</p>
    <p>pour modification git add index.html</p>
    <p>git log pour regarder qui a modifier et autre</p>
    <p>git branch "name" création d'une branche </p>
    <p>git branch pour voir dans qu'elle branche nous sommes</p>
    <p>git checkout pour changer de branche</p>

    <p>Basculement de la branche master -> git merge -> puis la supprimer </p>
    <p> git remote add origine + url</p>
    <p>git remote</p>